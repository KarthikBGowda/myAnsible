---
  - hosts: mywebservers
    become: yes
    gather_facts: yes
    tasks:
      - name: Installing Apache server in node
        apt:
          name: apache2
          state: present
        when: ansible_as_family=="Debian"